---
---

<section id="how-it-works" class="how-it-works">
  <div class="overlay"></div>
  <div class="container">
    <h2>How It <span class="gradient-text">Works</span></h2>
    <div class="steps-grid">
      <div class="step card">
        <div class="step-number">1</div>
        <h3>Book an Intro Call</h3>
        <p>Schedule a brief consultation to discuss your automation needs and goals.</p>
      </div>

      <div class="step card">
        <div class="step-number">2</div>
        <h3>Onboarding & Strategy</h3>
        <p>We'll develop a tailored automation strategy aligned with your business objectives.</p>
      </div>

      <div class="step card">
        <div class="step-number">3</div>
        <h3>Implementation & Weekly Consulting</h3>
        <p>Regular check-ins and updates as we implement and optimize your automation solutions.</p>
      </div>
    </div>
    <div class="cta-container">
      <a href="https://calendly.com/samrexford" target="_blank" rel="noopener noreferrer" class="btn">Book an Intro Call</a>
    </div>
  </div>
</section>

<style>
  .how-it-works {
    position: relative;
    padding: 8rem 0;
    background-image: url('/robot-creation.webp');
    background-size: cover;
    background-position: center;
    background-attachment: fixed;
    color: white;
  }

  .overlay {
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    bottom: 0;
    background: linear-gradient(
      to bottom,
      rgba(var(--background), 0.9),
      rgba(var(--background), 0.7)
    );
  }

  .container {
    position: relative;
    z-index: 1;
  }

  h2 {
    text-align: center;
    font-size: clamp(2rem, 4vw, 3rem);
    margin-bottom: 4rem;
  }

  .steps-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
    gap: 2rem;
    max-width: 1200px;
    margin: 0 auto;
  }

  .step {
    padding: 2rem;
    background: rgba(var(--card-bg), 0.9);
    border: 1px solid rgba(var(--primary), 0.2);
    display: flex;
    flex-direction: column;
    align-items: center;
    text-align: center;
  }

  .step-number {
    width: 48px;
    height: 48px;
    background: var(--primary-gradient);
    border-radius: 50%;
    display: flex;
    align-items: center;
    justify-content: center;
    font-size: 1.5rem;
    font-weight: 700;
    margin-bottom: 1.5rem;
  }

  h3 {
    font-size: 1.5rem;
    margin-bottom: 1rem;
    color: rgb(var(--text));
  }

  p {
    color: rgba(var(--text), 0.9);
    line-height: 1.6;
  }

  .cta-container {
    text-align: center;
    margin-top: 3rem;
  }

  @media (max-width: 768px) {
    .how-it-works {
      background-attachment: scroll;
    }
  }
</style>