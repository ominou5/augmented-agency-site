---
---

<section id="hero" class="hero">
  <div class="container">
    <h1>Your Business Augmented <span class="gradient-text"> With Custom AI Solutions</span></h1>
    <p>Leverage the power of automation and artificial intelligence to scale your operations</p>
    <div class="cta-buttons">
      <a href="https://calendly.com/samrexford" target="_blank" rel="noopener noreferrer" class="btn">Book an Intro Call</a>
    </div>
  </div>
</section>

<style>
  .hero {
    min-height: 90vh;
    display: flex;
    align-items: center;
    text-align: center;
    padding: 8rem 0 4rem;
    background: radial-gradient(circle at top right, rgba(var(--primary), 0.1), transparent 50%);
  }

  h1 {
    font-size: clamp(2rem, 5vw, 4rem);
    line-height: 1.2;
    margin-bottom: 1.5rem;
    padding: 0 1rem;
  }

  p {
    font-size: clamp(1rem, 2vw, 1.3rem);
    color: rgba(var(--text), 0.8);
    max-width: 600px;
    margin: 0 auto 2rem;
    padding: 0 1rem;
  }

  .cta-buttons {
    display: flex;
    gap: 1rem;
    justify-content: center;
    flex-wrap: wrap;
    padding: 0 1rem;
  }

  .btn-secondary {
    background: transparent;
    border: 1px solid rgba(var(--primary), 0.5);
    color: rgb(var(--text));
    padding: 0.8rem 1.5rem;
    border-radius: 0.5rem;
    font-weight: 500;
    cursor: pointer;
    transition: all 0.3s ease;
    white-space: nowrap;
  }

  .btn-secondary:hover {
    border-color: rgba(var(--primary), 1);
    background: rgba(var(--primary), 0.1);
  }

  @media (max-width: 480px) {
    .hero {
      padding: 6rem 0 3rem;
    }

    .cta-buttons {
      flex-direction: column;
      width: 100%;
      max-width: 300px;
      margin: 0 auto;
    }
  }
</style>